# src/include/gvcam — Public Headers

Platform-independent C headers installed with the SDK.

| Header | Purpose |
|---|---|
| `frame.h` | Frame descriptor (`gvcam_frame_desc_t`), pixel formats, buffer sizing |
| `ipc.h` | IPC channel abstraction (`gvcam_ipc_t`) — open, read, write, close |
